# Backend package

Install and run from this directory. Full documentation (product context, Docker Postgres, env vars, auth API, RBAC models, tests) is in the **[repository root README](../README.md)**.
